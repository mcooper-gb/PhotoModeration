# Major versions are capped so a rebuild of an existing tag cannot pull an untested
# major. This is not a lockfile: there is no CI to validate a new major against, so the
# cap is what stops one arriving unannounced in a release image.
nudenet>=3.4.2,<4
opencv-python-headless>=4.13,<6
python-dotenv>=1.2.2,<2
onnxruntime>=1.23,<2
numpy>=2.4,<3
watchdog>=6,<7
Pillow>=12.3.0,<13
Flask>=3.1,<4
waitress>=3,<4
psycopg[binary]>=3.3,<4
